import styled from 'styled-components'

const ListContent =  styled.ul`
 overflow-x: scroll;
 display: flex;
 white-space: nowrap;
 gap: 24px;
    `;
const ListItem = styled.li`
display: inline-block;

 
   `;
export default {ListContent, ListItem} 