export default interface MovieType {
    title: string;
    description: string;
    genre: string;
    highlighted: boolean;
    id: string;
    poster: string;
    rating: number;
    thumbnail: string;
  }

  export default interface GenreType {
    id: string;
    name: string;
  }